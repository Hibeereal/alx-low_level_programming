#include <stdio.h>

/**
 * main - Entry point "write a program that print number
 * of base 10 starting from 0"
 *
 * Return: Always 0 (success)
 */

int main(void)

{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
