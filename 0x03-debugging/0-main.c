#include <unistd.h>
#include "main.h"

/**
 * positive_or_negative - Entry point "tests function that prints if integer is positive
 * or negative"
 * @i: value to be checked
 * Return: Always 0
 */

int main(void)
{
	int i;

	i = 0;
	
	positive_or_negative(i);
	return (0);
}
